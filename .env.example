# 服务器配置
PORT=3000

# 数据库配置
DB_TYPE=json  # 可选：json 或 mysql
JSON_DB_PATH=./data/fixtures.json

# MySQL配置（仅当DB_TYPE=mysql时需要）
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_DATABASE=jig_monitoring
DB_PORT=3306

# 日志配置
LOG_LEVEL=info